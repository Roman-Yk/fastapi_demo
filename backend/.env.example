# =================================================================
# FastAPI Demo Backend Environment Variables - Example
# =================================================================
# Copy this file to .env and update with your values

# =================================================================
# Database Configuration (PostgreSQL)
# =================================================================
POSTGRES_DB=fastapi_demo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme  # CHANGE THIS IN PRODUCTION
POSTGRES_HOST=database      # Use 'localhost' when running without Docker
POSTGRES_PORT=5432

# =================================================================
# PgAdmin Configuration (Optional - for database management)
# =================================================================
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=changeme  # CHANGE THIS

# =================================================================
# Redis Configuration (for caching and Celery)
# =================================================================
REDIS_URL=redis://redis:6379/0  # Use 'redis://localhost:6379/0' when running without Docker

# =================================================================
# Order Service Type Configuration
# =================================================================
# Reload Car-Car Service
RELOAD_CAR_CAR__FULL_NAME=Reload Car-Car
RELOAD_CAR_CAR__SHORT_NAME=C-C

# Reload Car-Terminal-Car Service
RELOAD_CAR_TERMINAL_CAR__FULL_NAME=Reload Car-Terminal-Car
RELOAD_CAR_TERMINAL_CAR__SHORT_NAME=C-T-C

# Into Plukk Storage Service
INTO_PLUKK_STORAGE__FULL_NAME=Into Plukk Storage
INTO_PLUKK_STORAGE__SHORT_NAME=Plukk

# =================================================================
# Application Configuration
# =================================================================
FILES_PATH=files  # Directory for uploaded files

# =================================================================
# Development Notes
# =================================================================
# When running the application locally without Docker:
# - Set POSTGRES_HOST=localhost
# - Set REDIS_URL=redis://localhost:6379/0
# - Ensure PostgreSQL and Redis are running on your machine